const baseUrl = "http://localhost:8000";

const urls = {
  USER_LOGIN_URL: baseUrl + "/auth/user/login",
  ADMIN_LOGIN_URL: baseUrl + "/auth/admin/login",
  GET_CURRENT_USER: baseUrl + "/user",
};

export default urls;
